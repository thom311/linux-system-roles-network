---
- hosts: network-test
  vars:
    network_connections:
      - name: prod1
        type: ethernet
        mac: "{{ network_mac }}"
        ip:
          address:
            - 192.168.77.5/24

  roles:
    - network
